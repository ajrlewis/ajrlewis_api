{
    "env": {
        "PYTHONPATH": "api/"
    },
    "rewrites": [{
        "source": "/api/routers(.*)",
        "destination": "/api/main"
    }],
    "functions": {
        "api/main.py": {
            "maxDuration": 60
        }
    }
}